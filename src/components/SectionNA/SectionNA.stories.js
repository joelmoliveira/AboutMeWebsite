import React from 'react';
import SectionNA from './SectionNA';

export default {
  title: 'Components/SectionNA',
  component: SectionNA,
};

const Template = (args) => <SectionNA {...args} />;

export const Default = Template.bind({});
